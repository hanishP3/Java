Challenge: The "Password Checker"
Let's combine yesterday's logic with today's methods.

Write a method called isValidPassword.

Return Type: boolean.

Parameter: String pass.

Logic: If the password length is greater than 8, return true. Otherwise, return false.

In your main, call the method and use an if statement to print "Access Granted" or "Access Denied" based on what the method returns.

Can you build this gatekeeper? You feel me?