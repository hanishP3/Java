Add the Constructor: Use the this keyword inside it.

Add a Static Variable: Add static int studentCount = 0;.

Update the Count: Inside the constructor, add studentCount++;.

In Main: * Create s1 and s2 using the new new Student("Name", 4.0) syntax.

Print Student.studentCount at the very end.

Why? Because name is unique to the object, but studentCount belongs to the whole school (the Class).